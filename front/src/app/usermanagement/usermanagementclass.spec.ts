import { Usermanagementclass } from './usermanagementclass';

describe('Usermanagementclass', () => {
  it('should create an instance', () => {
    expect(new Usermanagementclass()).toBeTruthy();
  });
});
